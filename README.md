# c-doubly-linked-list
-Doubly linked list with an insert function written in C.  
-Standart doubly linked list. Function to insert new node to list before the the node with the specified index.  
-test.c, testData.txt, test.txt - files needed to run the test.c program to assert that the functions contained in functions.c are in-order and working properly.
